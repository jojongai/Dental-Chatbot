"""
Tool stubs — deterministic functions called by the chatbot orchestration layer.

Each function has a fully-typed signature and a TODO body.
The orchestration layer calls these by name (see TOOL_REGISTRY in schemas/tools.py).
"""
